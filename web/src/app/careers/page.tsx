export const metadata = { title: "Careers" };

export default function Page() {
  return (
    <section className="shell flex min-h-[70vh] flex-col justify-center py-[var(--section-y)] pt-[calc(var(--header-h)+6rem)]">
      <p className="text-xs font-medium tracking-caps text-ink-subtle uppercase">Careers</p>
      <h1 className="mt-6 max-w-[16ch] text-4xl font-normal text-balance text-ink">Join our team at Hazeberg</h1>
      <p className="mt-6 max-w-[48ch] text-sm text-ink-muted">
        This page is scaffolded so the navigation resolves. Its section build has not started.
      </p>
    </section>
  );
}
